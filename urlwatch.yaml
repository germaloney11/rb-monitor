display:
  new: true
  error: true
  unchanged: false
  empty-diff: false

report:
  ntfy:
    enabled: true
    topic_url: "https://ntfy.sh/redbooks-bookdrop-xyz123"   # ← CHANGE THIS to your real ntfy topic
    priorities:
      changed: high
      new: high
      error: default
